// Procedural
procedural(valor1, valor2, valor3); // foco nas funções com dados

// OO
objeto = {
  valor1,
  valor2,
  valor3,
  procedural(valor1, valor2) {
    // ...
  },
};

objeto.procedural(); // Foco nos dados com funções

// Principios
// 1: abstração (traduzir as coisas do mundo em objetos)
// 2: encapsulamento (mostra o que precisa pra quem precisa sem mostrar os detalhes desnecessários)
// 3: herança (reuso de codigo)
// 4: polimorfismo (multiplas formas| abstrair)
